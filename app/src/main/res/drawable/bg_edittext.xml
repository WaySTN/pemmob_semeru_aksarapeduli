<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Solid color -->
    <solid android:color="@color/blue_light"/> <!-- Warna biru -->

    <!-- Rounded corners -->
    <corners android:radius="16dp"/> <!-- Sudut melengkung -->

    <!-- Stroke/border (opsional) -->
    <stroke
        android:width="1dp"
        android:color="@color/blue_dark"/> <!-- Warna border lebih gelap -->
</shape>