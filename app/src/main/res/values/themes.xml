<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Aksaraaa" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <!-- Warna untuk edge-to-edge -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item> <!-- Untuk teks status bar gelap -->
        <item name="android:windowLightNavigationBar">true</item> <!-- Untuk teks nav bar gelap -->
    </style>

    <style name="Theme.Aksaraaa" parent="Base.Theme.Aksaraaa" />
</resources>